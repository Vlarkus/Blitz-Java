- CanvasPanel allows to zoom in/out.
- CanvasPanel displays mouse field position in bottom left corner.
- When hovering over a tool for more than 2 seconds, the tool will show a small suggestion window.
- Add Help option in MenuBar containing Tutorials.
- Mouse changes cursor depending on the tool inside CanvasPanel.

- ControlPoint can be continuous or not. isContinuous()? thetaEnd = thetaStart + 180; Add option to turn it on an off in InfoPanel.
- Add button to InfoPanel to turn helper points off: set rStart and rEnd to 0 and not show text fields for them.
    add private boolean isCurved.

- CanvasPanel add check for selected pointer if its relatedControlPoint is not locked!!!
- Use border for each panel instead of text.